# Cloud backup is optional — the app works fully offline without these.
# See README.md "Cloud backup setup" for how to get these two values.
# Copy this file to .env.local and fill them in; .env.local is gitignored.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
